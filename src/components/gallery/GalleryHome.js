import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const GalleryHome = () => {
  return (
    <View>
      <Text>GalleryHome</Text>
    </View>
  );
};

export default GalleryHome;

const styles = StyleSheet.create({});
